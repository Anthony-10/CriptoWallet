import 'package:flutter/material.dart';


class CoinsSearchView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Coins Search'),
      ),
      body: Container(),
    );
  }
}
